import { PlaceholderDiorama } from './PlaceholderDiorama'

/**
 * Chapter 3 — The Garage Order (deep green). Placeholder marker.
 * Final dressing: the actual Jobs family garage with cardboard boxes,
 * hand-soldered Apple I units, an order ledger on a card table. Drag-to-assemble
 * mechanic for an Apple I.
 */
export function Chapter3Garage() {
  return <PlaceholderDiorama chapterId="ch3" />
}
