import { PlaceholderDiorama } from './PlaceholderDiorama'

/**
 * Chapter 6 — The Now (soft daylight). Placeholder marker.
 * Final dressing: balcony / atrium with natural light, a single object on a
 * plinth (current iPhone or Vision Pro), shadow-puppet silhouettes through a
 * window. Closing line + "Thank you for visiting." Credits scroll.
 */
export function Chapter6Now() {
  return <PlaceholderDiorama chapterId="ch6" />
}
